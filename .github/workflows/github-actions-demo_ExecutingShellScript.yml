name: GitHub Actions Demo - Executing Shell Script
run-name: ${{ github.actor }} is testing out GitHub Actions - Shell Script Execution
on: [push]
jobs:
    Explore-GitHub-Actions-Shell-Script:
        runs-on: ubuntu-latest
        steps:
            - run: echo "This job was automatically triggered by a ${{ github.event_name }} event"
            - run: echo "This job is now running on a ${{ runner.os }} server hosted by GitHub!!" 
            - run: echo "The name of your branch is ${{ github.ref }} and your repository name is ${{ github.repository }}."
            - name: Checkout repository code on runner
              uses: actions/checkout@v4
            - run: echo "The ${{ github.repository }} cloned to the runner"
            - run: echo "Workflow is now ready to test your code on runner"
            - name: List the files in repository
              run: ls -lrta
            - name: Excuting the Schell Script
              run: |
               chmod +x ascii-script.sh
               ./ascii-script.sh


